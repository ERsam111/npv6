import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const InputDataTable = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Input Data</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Input data table will be configured here.</p>
      </CardContent>
    </Card>
  );
};
